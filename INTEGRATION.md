# 公网中转接入指南（客户端侧）

给要 Gosslan 客户端接线的人。读完这一份就能把中继链路写对，不需要去读 `server.mjs`
—— 但**必须**遵守第 3 节的硬性要求，其中几条违反了不会出现任何报错，只会静默连不上。

服务器侧的一切都以 [`server.mjs`](server.mjs) 为准，本文与它逐条对齐；协议规格另见
[README 的协议规格一节](README.md#协议规格)。

- 适用版本：`GSRL1`（首行协议）。服务器对不认的版本一律拒绝，不会降级。
- 本文写的是**必须怎么做**。第 9 节列了"不要指望这里改"的东西。
- 文中每条"会看到什么"都是 2026-09-22 在 macOS arm64 / Node v24 上实测过的，不是推演。

---

## 客户端现在到哪一步了（2026-09-22 核对主仓工作区）

拨号器**已经在写了**，不是从零开始。对照本文要求逐条给结论，省得重复劳动。
**证据一律按符号（函数/常量名）指，不写行号** —— 主仓这些文件当天还在改，行号当天就失效
（实测：`GSRL1` 那行在本次核对期间从 271 行移到了 299 行）。

| 本文要求 | 主仓现状 | 证据（按符号找） |
|---|---|---|
| ② 发 `GSRL1` 首行 | ✅ 已有 | `network/transport/relay.rs` 的 `relay_negotiate` 第①步 |
| ③ `GSRW1` 协商 + 记录层密封 | ✅ 已有 | 同文件：`build_wrap_offer` → `enable_seal`（`TcpSender`/`TcpReceiver` 各一次） |
| **要求 1** 同一通道只许一条连接 | ✅ 已满足 | `relay_rendezvous_task` 先用 `pick_relay_targets` 过滤已连通好友；`connect_to_peer` 的 `DialGuard` 在途键是 `peer:{id}` 而非 `ep:{addr}`（**按身份不按地址**，正是主仓 ADR-0020 的 D7） |
| 要求 2 自建协商看门狗 | ✅ 已满足 | 常量 `RELAY_NEGOTIATE_TIMEOUT_SECS = 10` |
| 要求 3 重拨节奏 | ⚠️ 能跑，但**占空比约 50%** | `RELAY_RENDEZVOUS_SECS = 10` 一轮、每轮又被上面那个 10s 看门狗封顶 ⇒ 有效会合窗口是 10s 不是 30s，相位错开时会空转几轮（见要求 3） |
| 要求 6 只对已绑定公钥的好友 | ✅ 已满足 | `relay_rendezvous_task` 读 `list_bound_friend_identities` |
| 要求 7 口令拒空白 | ✅ 已满足 | `commands/relay.rs` 的 `validate_relay_token`（空白会让服务器字段数错乱） |
| 每设备电路上限 | ✅ 有 | `MAX_RELAY_CIRCUITS = 8` → 与服务器 `MAX_CLIENTS=128` 的算术见 README「配置项」下的算例 |
| **要求 5 换天（±1 天哈希）** | ❌ **未做** | `relay_rendezvous_task` 只算当天：`relay_epoch_day(db::now_ms())`，换天窗口两端各算一天会配不上 |
| **要求 8 / 设置页 UI** | ❌ **未做** | `src/` 里 `relay_server`、"公网中转" 零命中；后端 `commands/relay.rs` 已就绪 |
| 真机跨网联调 | ❌ 未做 | 目前只有单测（含一个自建的最小假中继） |

**所以下面这些活是真的剩着**：设置页开关 + 两个输入框（含口令走安全存储）、换天窗口的
处理（要求 5）、把 1.1 那三种失败状态拆进诊断（要求 2，服务器侧零成本）、复核一下
会合占空比（要求 3）、以及拿两台真机跨网跑通一次。其余照本文核对即可，不要另起一套。

---

## 0. 全貌：你要做的事就这么多

```
        你的设备                         中继服务器（本项目）
   ┌────────────────┐                ┌──────────────────┐
   │ 算通道哈希 ch    │                │                  │
   │ TCP 连上 :59993 │ ─── 拨号 ────► │ 校验口令 + 格式    │
   │ 发一行首行       │ ─── GSRL1…  ─► │ 按 ch 找等待者     │
   │                 │ ◄─ 无回复 ── │ （仅关闭时机可辨）  │
   │ 发协商线 GSRW1   │ ═══ 密文 ═══► │                  │
   │ 之后：记录层帧    │ ═══ 密文 ═══► │ 只搬字节，不解析   │
   └────────────────┘ ◄══ 密文 ══════ └──────────────────┘
```

服务器唯一读的一次数据就是那一行首行。之后的字节它连"一条消息的边界在哪"都不需要知道。

---

## 1. 三条不能协商的事实

1. **服务器在首行之后永远不回任何"内容"** —— 没有"注册成功"，没有"对端已到位"，没有错误码。
   但它有一个**旁路信号**：见下面 1.1，那个信号值得用起来，只是它不是"回复协议"。
2. **它只认通道哈希，不认设备**。两条登记了同一串哈希的连接就会被拼在一起 —— 它没有能力
   判断这两条是不是同一台设备拨的。这条直接产生第 3 节的要求 1。
3. **它不存任何东西**。对端不在线就没有人替你收消息；进程重启所有在途电路立刻断。
   任何"离线可达"的需求都不属于这台服务器（见第 9 节）。

### 1.1 关闭时机 = 服务器唯一的回复（用起来，但别把它当协议）

服务器的行为是确定的两种命运，**差两个数量级，跨公网也分辨得清**：

| 服务器做了什么 | 你会观察到 | 含义 |
|---|---|---|
| 立刻 `destroy()`（毫秒级） | 首行写完后马上 `ECONNRESET` / EOF | **首行被拒** |
| 把你放进等待表 | 连接一直开着，最长 `WAIT_TIMEOUT_MS`（默认 30s） | **首行已被接受**（准入通过） |

所以三件事里你能分开显示的是这三件，**不是四件**：

1. `TCP 连不上` = 服务器不可达 / 端口没放行 —— 纯本端就知道，不需要服务器配合；
2. `立刻被关` = 首行被拒。注意这是**三合一**：口令错、协议版本不匹配、格式非法，
   在服务器上走的是同一个 `reject()`。**精确原因只有服务器日志有**
   （`[reject] 首行非法（协议版本 / token / 通道哈希格式）`）。
   所以 UI 文案写"服务器拒绝准入：口令或版本不匹配，请核对服务器日志"，
   **不要写死"口令错误"** —— 版本不匹配会被误导成口令问题，正是要避免的那类误判。
3. `没被立刻关` = 准入通过、对端这一轮还没来。

⚠️ **判定第 3 种请用你自己的协商超时，不要等服务器关你**。现在客户端的
`RELAY_NEGOTIATE_TIMEOUT_SECS = 10` 比服务器 `WAIT_TIMEOUT_MS = 30s` **短**，
所以你总是先主动丢掉 socket，永远不会观察到那次 30s 关闭 —— 这没问题，10s 就够了。
只有当有人把客户端超时拉到 **大于** 服务器的 `WAIT_TIMEOUT_MS` 时，"对端没来"才改由
服务器告诉你；两者大小关系一旦反过来，这里的判据就得跟着改（同一条耦合还写在 §4 表里）。

**明确不存在的第四种**：`对端没开中继` 和 `对端不在线` 对哑管道是同一个观测（这个哈希没人
登记），原理上分不出来 —— 服务器不知道"应该有谁"，这正是 D2 用哈希而不是身份做会合凭据的
代价。别在 UI 上承诺它。


---

## 2. 接线五步

### ① 算通道哈希

```text
ch = SHA-256(
    "gosslan-relay-ch-v1"
    ‖ device_id₁ ‖ 0x00 ‖ ed25519_pub_b64₁ ‖ 0x00
    ‖ device_id₂ ‖ 0x00 ‖ ed25519_pub_b64₂ ‖ 0x00
    ‖ epoch_day(大端 u64)          // epoch_day = now_ms / 86_400_000，UTC 天序号
)   → 输出 64 个小写十六进制字符
```

- `(device_id₁, device_id₂)` 按 **device_id 的 ASCII 字典序**排列，公钥跟着各自的 ID 走。
  两端各自算，必然得到同一个值 —— 排序写错是"两端都显示已连接但永远配不上"的经典成因。
- 公钥用 **base64 字符串的字节**参与哈希，不是解码后的原始 32 字节（与 `relay_seal.rs` 一致）。
- 输出必须是**小写** hex：服务器用 `/^[0-9a-f]{64}$/` 校验，大写一律拒。

### ② 拨号并发首行（每条连接只有一次机会）

```text
GSRL1 <token> <ch>\n
```

| 约束 | 值 | 违反了 |
|---|---|---|
| 字段数 | 正好 3 个，单个空格分隔 | 首行非法，连接被关闭 |
| 首段常量 | 必须是 `GSRL1`（区分大小写） | 同上 |
| `token` | 与服务器的 `TOKEN` 完全一致，**不得含空格** | 同上 |
| `ch` | 64 位小写十六进制 | 同上 |
| 整行长度 | ≤ **256 字节**（含 `\n`） | `首行超长` |
| 发送时限 | TCP 连上后 **10 秒**内（`HELLO_TIMEOUT_MS`） | 首行超时 |
| 行尾 | 必须 `\n`；末尾多一个 `\r` 可容忍 | 找不到 `\n` 就按超长/超时处理 |

首行之后**不要再发第二个首行**。同一包里紧跟首行的字节会被当成电路载荷，服务器会替你
暂存并在配对后按序写给对端（实测：先到一方发的 `FROM-A` 在配对后被对端完整收到）。

### ③ 发协商线，建立你自己的加密通道

```text
GSRW1 <role A|B> <b64 临时 X25519> <b64 签名>\n
```

4 个字段，**不含长期公钥、不含 device_id**（这是刻意的：中继拿不到任何能反算通道的材料）。
签名覆盖 `("gosslan-relay-wrap-v1", 我的 device_id, 我期望的对端 device_id, 通道哈希,
role, 临时公钥)`。role 由 **device_id 字典序**决定，不由"谁先连上"决定 —— 否则两端会算出
不同的方向密钥。

**双向同时发**，不要"等一下看有没有对方"，那会死锁（服务器不会给你任何信号）。

### ④ 之后每帧走记录层

```text
4 字节大端长度 ‖ ChaCha20Poly1305( 8字节递增计数器 ‖ 4字节大端长度 ‖ JSON 帧 )
```

密钥 = `SHA-256("gosslan-relay-record-v1" ‖ X25519 共享密钥 ‖ ch ‖ 0x00 ‖ "a2b"/"b2a")`，
nonce = 4 字节零 ‖ 计数器。接收端要求计数器**严格 +1**：注入、重放、截断一律断链，
所以断链之后**必须整条电路重来**（含重新发协商线），不要试图在同一条连接上"恢复"。

再往里的 `JSON 帧` 与局域网里跑的**完全相同** —— 这个功能不需要任何新帧类型。

### ⑤ 保持与退出

- 心跳必须远短于服务器的 `IDLE_TIMEOUT_MS`（180 秒），见下表。
- 想断开就 `close()` 整条 socket。**不要只 `shutdown(SHUT_WR)`** —— 服务器看到 EOF 会
  立刻把整条电路两端一起拆掉（实测日志：`[circuit] 结束 … 原因=eof`）。

---

## 3. 硬性要求（违反了没有报错，只有"连不上"）

### 要求 1 ⚠️ 同一个通道哈希，同时只许存在一条自己这边的连接

**这是最容易踩、且最难查的一条。** 服务器只看哈希（第 1 节事实 2），所以如果你为了重拨
又开了一条、而上一条在服务端还活着，**这两条会被拼成一条自环电路**：你的字节绕一圈回到
你自己，真正的对端永远配不上，而**服务器日志显示一切正常**。

已实测复现（同一台机器对同一 ch 拨两条，两条互相收到了对方的字节）：

```
socket1 收到: [FROM-SOCKET-2]   socket2 收到: [FROM-SOCKET-1]
[circuit] 配对 ch=aaaaaaaa… 电路数=1      ← 日志没有任何异常
```

怎么做对：

- 重拨**必须先 drop 掉自己上一条 socket**（关掉、从表里摘掉），再拨新的。不能"新拨一条，
  旧的等它自己超时"。
- 去重键必须是**通道哈希 / 对端 device_id**，不能是"服务器地址"。按地址去重会导致
  "只连得上第一个好友"（主仓 ADR-0020 的 D7 就是这条）。
- 反过来也别按地址放过两条：判据是"同一个 ch 是否已有一条在自己手里"。
- **不要试图让服务器分辨"同一台设备的两条连接"**：按来源 IP 判会直接误伤
  "两台设备在同一个 NAT 后面"这个主场景。这条只能落在客户端。

### 要求 2 不要解析服务器的任何"消息"，但要把 1.1 的两种关闭时机用满

没有注册回执、没有错误码，失败判定归你自己的看门狗：首行发出后 N 秒（现在 10s）内没拿到
对端合法的 `GSRW1` ⇒ 判这一轮失败，**整条 socket 丢掉重拨**（回到要求 1）。

在此之上，把 1.1 那两种命运接进诊断信息里 —— 这是**免费**的，不需要服务器改一行：

```text
TCP 连不上            → "连不上中转服务器（检查地址/端口/安全组）"
首行后毫秒级被关       → "服务器拒绝准入：口令或版本不匹配（精确原因见服务器日志）"
活着但 10s 没人来      → "已连上服务器，对方这一轮还没接入（对方可能没开中继开关）"
```

第三条尤其值钱：它今天全部被折叠成一句"建链未成功"，是用户最容易误判成"服务器坏了"的地方。
反过来，**别去承诺区分"对方没开"和"对方不在线"** —— 那第四种这台服务器给不了（1.1 末尾）。

### 要求 3 会合窗口取的是"两端孰短"，调服务器那个 30s 基本没用

服务器侧的预算是 `HELLO_TIMEOUT_MS=10s`、`WAIT_TIMEOUT_MS=30s`（等不到对端就放弃这一路）。
但**真正决定会合成功率的是占空比**，公式是：

```text
单轮你在等待表里活着的时间 = min(你的 socket 存活期, WAIT_TIMEOUT_MS)
```

现在客户端的存活期被 `RELAY_NEGOTIATE_TIMEOUT_SECS = 10` 封顶 ⇒ **窗口是 10 秒，不是 30 秒**，
服务器那 30s 只在"客户端崩了 / 半开没读到 FIN"这种异常里才起作用。于是：

- 两台设备各按 10s 一轮、每轮只活 10s ⇒ 占空比约 50%，**相位错开时会空转几轮**才碰上。
  这是正常的，不是 bug；表现就是"两端都连上了但过一会儿才通"。
- 想让它更快更稳，**先动客户端这一侧**：单轮 socket 活久一点（≥ 会合周期），或缩短重拨间隔。
  把服务器 `WAIT_TIMEOUT_MS` 从 30s 调到 12s 只会**降低**成功率，不会提高 ——
  这条是 1.1 末尾那个耦合关系的直接推论。
- 30s 这个值本身也别往下调太狠：它同时要覆盖"跨公网拨号 + 首行往返"的最坏情况。

### 要求 4 对端来之前，别在这条连接上灌大数据

先到一方在等待配对期间，服务器最多替对端暂存 `PENDING_MAX`（默认 **1 MiB**），超过就直接
拒掉**这条连接**（`[reject] 等待期暂存超限`）。协商线 ≤512 字节，离这个线差三个数量级。

所以：**文件分片必须在协商完成、对端确认之后才允许进这条连接**（这本来也应该是要 ③ 的
前置条件）。另外配对后服务器不排队、不缓存，它靠 TCP 背压把慢的源头 `pause()` 住 ——
这意味着对端收得慢时你的写会自然变慢，不要在自己的发送侧再叠一个大缓冲。

### 要求 5 换天要能自处（通道哈希按 UTC 天轮换）

换天那一刻两端可能各算一天，于是配不上。两个办法，选一个：

- **被动**：靠重拨循环自愈。代价是换天边界处最长可能卡几分钟，日志上表现为"两端都连上了
  但配不成"，**别按故障查**。
- **主动（推荐）**：在 UTC 00:00 前后各 1 小时内，同时拨**今天**和**昨天**两个哈希。
  它们是不同通道，**不违反要求 1**（要求 1 禁的是同一个 ch 两条连接）；对端算的是哪天
  都可能，两个都试必然命中其中一个。窗口过后立刻收回成一条。

### 要求 6 只在局域网失败时才用它，且只对已绑定公钥的好友

- 链路优先级 **LAN > 跨网段/中继 > 蓝牙** 由客户端保证。填了中继**不能**让正常聊天绕道公网。
- 只给 **friends 表里已绑定 Ed25519 公钥**的对端建中继电路。协商线的验签就用那把公钥，
  **不认自报公钥** —— 这是"服务器冒充不了任何人"的依据，也是别拿中继给陌生人开洞的依据。
- 中继只是补上"进网"那一跳：外网第 6 个人通过中继连上局域网里任意一人后，就能和局域网里
  所有人收发（走的是 Gosslan 既有的多跳转发，不是中继在转发消息）。

### 要求 7 口令按敏感信息管理

`TOKEN` 是**唯一的准入手段**，明文跑在 TCP 上（这条链路没有 TLS，正文靠你自己的记录层加密）。
知道口令的人可以用这台服务器转发任意字节，但**读不到**内容（第 1 节）。要求：

- 不写日志、不进崩溃上报、不出现在诊断面板的截图字段里。
- 存进系统的安全存储（macOS Keychain / Windows 凭据管理器 / Android Keystore），
  不要放明文配置。
- 换口令 = 所有客户端都要重填，所以别把它做成能静默轮换的东西。

### 要求 8 地址形态

目前只支持 **IPv4 字面量 `地址:端口`**（不支持域名解析），默认端口 **59993/tcp**。
域名解析要放客户端做，服务器不解析任何主机名。

---

## 4. 超时与预算（服务器侧的值就是契约）

| 服务器参数 | 默认 | 客户端该怎么配 |
|---|---|---|
| `HELLO_TIMEOUT_MS` | 10 000 | TCP 连上后 10s 内必须把首行写完（含 `\n`） |
| `WAIT_TIMEOUT_MS` | 30 000 | **有效窗口 = min(你的 socket 存活期, 30s)**，见要求 3；别指望靠调小它来改善会合 |
| `IDLE_TIMEOUT_MS` | 180 000 | **心跳间隔必须 ≪ 180s**。现网心跳 5s，安全 |
| `PENDING_MAX` | 1 048 576 | 配对前累计发送 < 1 MiB（要求 4） |
| `MAX_CLIENTS` | 128 | 一条电路 = 两条连接 ⇒ 128 条连接 = 64 对。**30 人的真实分布约每人 1 条电路**（进网一条即可靠多跳触达全网）⇒ ~30 条连接，宽裕；最坏（人人都在网外且各有 ≥8 个需中继的好友）是 `30×8/2=120` 电路 = **240 条连接，会撞闸门**。撞了会在日志明说，且**调大它必须同步调小 `PENDING_MAX`**（两者相乘就是这台机器的内存上界，见 README 配置项那条注） |
| 空闲/心跳流量 | — | 每条约 300 B / 5 s ⇒ 30 条电路 ≈ 24 kbps，可忽略；**上行瓶颈在云主机带宽**，1 Mbps 传 100 MB 要 13 分钟 |

---

## 5. 失败了长什么样（服务器日志 ↔ 客户端该做什么）

| 服务器日志 | 你在客户端会看到 | 该做什么 |
|---|---|---|
| `[reject] 首行非法（协议版本 / token / 通道哈希格式）` | 连上后立刻被关 | 查口令一致性、字段数、hex 是否小写、整行是否 ≤256 B |
| `[reject] 首行超时` | 10s 后被关 | 检查你是否真的发了 `\n`（不是 `\r\n` 之外还漏了换行） |
| `[reject] 等待期暂存超限` | 被关 | 违反要求 4：对端没来就发了大数据 |
| `[reject] 连接数已达上限` | 连上就断，且**没有**上面任何一行 | 服务器满了，不是你错了（规模算术见 §4 最后一行） |
| `[wait] 对端未出现，放弃 ch=…` | 30s 后被关（**你多半看不到**：你自己的 10s 看门狗先到） | 正常。继续重拨；这条日志的主要价值是在服务器侧证明"首行已被接受"，即 1.1 的第三种状态 |
| `[circuit] 配对 ch=…` 但两端都没流量 | 连接开着、字节回到自己 | **违反要求 1（自环）**，或两端角色/密钥方向算反了 |
| `[circuit] 结束 … 原因=eof` | 对端或本端关了写 | 本端别 `SHUT_WR`（见 ⑤）；否则是正常退出 |
| `[circuit] 结束 … 原因=idle` | 180s 无字节后被关 | 心跳间隔太长或链路真的死了 |
| `[circuit] 结束 … 原因=peer-gone` | 一端断了，两端一起退 | 两端各自重拨，会重新会合 |

---

## 6. 参考伪代码（一轮拨号，正确的重拨顺序）

```text
loop（每 5~10 秒一轮）:
    if 中继开关关闭 或 局域网已连通: 睡眠并继续
    for ch in 今日哈希 (+ 换天窗口内的昨日哈希):
        if 本机已有一条针对 ch 的连接: continue          # 要求 1 的守卫
        sock = tcp_connect(relay_ip:59993)
        sock.send("GSRL1 " + token + " " + ch + "\n")     # 只发一次
        登记 ch -> sock                                    # 立刻登记，防并发双拨
        发自己的 GSRW1 协商线（不等对方）                   # 要求 2
        if 15s 内没收到对端合法 GSRW1:
            drop sock; 摘掉 ch -> sock                     # ★ 先关旧的，下一轮才允许再拨
            continue
        进入记录层收发；计数器不连续/验签失败 ⇒ 整条丢弃重拨（不试图恢复）
```

关键就在那颗星：**先 drop，再拨下一条**。

---

## 7. 本地联调（不需要真服务器，5 分钟）

```bash
# 终端 1：起一个本地服务器（口令固定，便于手搓）
TOKEN=test PORT=59993 HOST=127.0.0.1 node server.mjs

# 造一个合法的通道哈希（64 个小写 hex）
CH=$(printf 'a%.0s' $(seq 64))
```

```bash
# 终端 2（先到的一方，先发数据也没关系 —— 服务器会替你暂存）
( printf "GSRL1 test $CH\nFROM-A\n"; sleep 3 ) | nc 127.0.0.1 59993
# 终端 3（30 秒内到）
( printf "GSRL1 test $CH\nFROM-B\n"; sleep 3 ) | nc 127.0.0.1 59993
```

期望（实测输出）：终端 2 打出 `FROM-B`、终端 3 打出 `FROM-A`，服务器日志
`[circuit] 配对 …` 然后 `结束 … 原因=eof`。

上面那种 `sleep` 撑住 stdin 的写法是故意的：**管道一读到底就 EOF，服务器看到 EOF 会立刻
拆掉整条电路**，普通 `printf … | nc` 收不到东西。macOS 的 BSD `nc` 没有 `-q`（Linux 的
GNU netcat 用 `-N`，但 `-N` 会半关，同样被服务器当成退出）。

跑服务器自己的回归：`TOKEN=*** node selftest.mjs`（9 条）。它包含"载荷不得出现在
服务器输出里"这条可证伪断言，改协议时请顺带加用例。

---

## 8. 提 PR 前自查

- [ ] 同一 ch 同时只有一条自己的连接（要求 1），重拨前已 drop 旧 socket
- [ ] 去重键是 ch / 对端 device_id，不是服务器地址
- [ ] 不解析服务器的任何"消息"，但 1.1 的三种失败状态已分开展示（要求 2），且有自建协商看门狗
- [ ] 首行一次、≤256 B、hex 小写、`\n` 结尾
- [ ] 配对前不发送超过 1 MiB（要求 4）
- [ ] 心跳 ≪ 180s；断链后**整条重来**，不在原连接上恢复
- [ ] 局域网正常时中继不参与；只给已绑定公钥的好友建电路（要求 6）
- [ ] 口令不入日志/上报，存安全存储（要求 7）
- [ ] 换天窗口行为已确认（要求 5，二选一）
- [ ] 断网/服务器重启时 UI 不谎报"已连接"，且 outbox 会重投

## 9. 不要指望这里改的东西

| 你想要的 | 为什么不在这里做 |
|---|---|
| 对方不在线也能收消息 | 必须落盘，与"这台机器不存任何用户数据"直接冲突。现有方案是发送方本地 outbox 保留 7 天重投 |
| "对端已到位" 回执 / 错误码 | 一回执就多出一个可被利用的状态面。**免费的那半已经在 1.1 里了**（立刻关 vs 被保持），够把三种失败分开；缺的是客户端去读，不是服务器去说 |
| 一条 `GSRS1` 能力/版本问候行 | 只有一个版本，且版本号已在首行里 —— 没有真分叉要协商。而它要求服务器在**对方出示口令之前**就说话，等于给公网扫描器发横幅；客户端 `relay_negotiate` 开头注释也明写"它没有回复协议"，加这行是双仓协议变更，不是服务器侧小补丁 |
| "某个通道现在注册了谁" 这类查询 | 那是一台 (pair, day) 成员预言机，会直接废掉"通道哈希按天轮换"这个假名设计。聚合计数（接入/配对/拒绝/字节）已经在 `[stats]` 行里，够运维用 |
| 按 token 的配额或限速 | 30 人**共用同一个 `TOKEN`**，口令粒度上没有可区分主体 ⇒ per-token 配额就是 `MAX_CLIENTS` 换个名字。源 IP 令牌桶会引入这个文件第一个真状态机，破掉"全部状态就是一个 `Map` 加一个 `Set`"。端口扫描该在 nftables `recent` / fail2ban 治，位置也在下面那层 |
| 一对多扇出（群聊走中继时只拨一条） | 要求服务器知道群成员（撞 D2），且多写扇出会让一个慢成员把源头 `pause()` 死 —— 现在"内存上限就是两端 TCP 缓冲"这个干净性质就没了。**群聊不缺这个**：一条电路进网后，群消息走 Gosslan 既有的多跳洪泛 |
| 服务器帮我分辨同一设备的两条连接 | 只能靠源 IP，会误伤同 NAT 的两台设备（正是主场景） |
| 一条连接服务多个好友（多路复用） | 那是"控制连接"级别的协议改造，独立一轮，不是这里的小改 |
| 隐藏流量形状/精确长度/时刻 | 要么 TLS，要么定长分桶填充，都是独立一轮。现在做属于过度设计 |
| 走 443 / WebSocket 绕过运营商限制 | 会引入依赖与证书管理。若确实有需求，单开一轮讨论，别塞进 `GSRL1` |
